import * as React from "react";
import Props from "./props";
import Style from "./style";

const Component = (props: Props) => {
  return (<header style={Style.root}></header>);
};

export default Component;